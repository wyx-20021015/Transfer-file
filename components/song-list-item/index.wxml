<!--components/song-list-item/index.wxml-->
<view class="item">
  <view class="index">
    {{indey+1}}
  </view>
  <view class="info">
    <view class="name">
      {{info.name}}
    </view>
    <view class="source">
      <image class="icon" src="/assets/images/icons/sq_icon.png"></image>
      <text>{{info.ar[0].name||info.artists[0].name}} </text>
      <text wx:if="{{info.alia[0]||info.alias[0]}}" class="dots">·</text>
      <text wx:if="{{info.alia[0]||info.alias[0]}}">{{info.alia[0]||info.alias[0]}}</text>
    </view>
  </view>
</view>